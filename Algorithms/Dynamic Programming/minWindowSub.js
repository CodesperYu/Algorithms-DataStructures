const minWindowSub = (str, sub) => {
	let start = null;
	let startLetter = null;
	let end = null;
	let counter = 0;
	let coords = null;
	let shortest = Infinity;
	let memory = {};

	for (let i = 0; i < str.length; i++) {
		let letter = str[i];
		if ()
	}

	if (shortest !== Infinity) {
		return minWindowSub.slice([...coords])
	} 
		return '';
	
};

console.log(minWindowSub('ADOBECODEBANC', 'ABC'));
